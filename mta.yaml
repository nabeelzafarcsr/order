_schema-version: "3.2"
ID: comcsrorder
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comcsrorder-destination-content
  type: com.sap.application.content
  requires:
  - name: comcsrorder-destination-service
    parameters:
      content-target: true
  - name: comcsrorder-repo-host
    parameters:
      service-key:
        name: comcsrorder-repo-host-key
  - name: comcsrorder-uaa
    parameters:
      service-key:
        name: comcsrorder-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comcsrorder_html_repo_host
          ServiceInstanceName: comcsrorder-html5-service
          ServiceKeyName: comcsrorder-repo-host-key
          sap.cloud.service: comcsrorder
        - Authentication: OAuth2UserTokenExchange
          Name: comcsrorder_uaa
          ServiceInstanceName: comcsrorder-xsuaa-service
          ServiceKeyName: comcsrorder-uaa-key
          sap.cloud.service: comcsrorder
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comcsrorder-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comcsrorder-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comcsrorder.zip
      name: comcsrorder
      target-path: resources/
- name: comcsrorder
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comcsrorder-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comcsrorder-destination-service
    service-plan: lite
- name: comcsrorder-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: comcsrorder-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: comcsrorder-xsuaa-service
    service-plan: application
- name: comcsrorder-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comcsrorder-html5-service
    service-plan: app-host
- name: comcsrorder-connectivity
  type: org.cloudfoundry.managed-service
  parameters:
    service: connectivity
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
